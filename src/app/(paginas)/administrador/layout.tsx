export default function AdministradorlLayout({ children }: { children: React.ReactNode }) {
    return (
        <div>
            <h1>Administrador layout</h1>
            {children}
        </div>
    );
}